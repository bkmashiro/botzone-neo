/**
 * 评测结果枚举
 *
 * 纯领域对象，零依赖。
 */

/** 通用评测结果 */
export enum Verdict {
  /** 正常 */
  OK = 'OK',
  /** 编译错误 */
  CE = 'CE',
  /** 超时 */
  TLE = 'TLE',
  /** 内存超限 */
  MLE = 'MLE',
  /** 运行时错误 */
  RE = 'RE',
  /** 沙箱错误（nsjail 自身问题） */
  SE = 'SE',
  /** 无结果（Bot 未给出有效输出） */
  NR = 'NR',
  /** 答案错误（OJ 专用） */
  WA = 'WA',
  /** 答案正确（OJ 专用） */
  AC = 'AC',
}
